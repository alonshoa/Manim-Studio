Baseline notes for `recursion/count_bottles`.

Expected review frames should show the box-and-bottles metaphor, the empty-box
base case, the same-machine recursive call on the rest of the box, and the
return flow ending at 4.
