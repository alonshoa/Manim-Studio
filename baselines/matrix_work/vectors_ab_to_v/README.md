# Vectors AB To V Baseline

Render command:

```bash
manim-slides render -ql decks/matrix_work/vecs_slides.py VectorsABtoV
```

Capture representative frames for the title, axes, vector addition, and final loop slide.
