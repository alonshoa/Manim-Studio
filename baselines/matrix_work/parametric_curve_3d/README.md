# Parametric Curve 3D Baseline

Render command:

```bash
manim -ql decks/matrix_work/parametric_curve_3d.py ParametricCurve3D
```

Capture representative frames for the initial axes, mid-curve reveal, and final helix.
