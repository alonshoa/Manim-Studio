# Binary Cross Entropy Baseline

Render command:

```bash
manim -ql decks/losses/cross_entropy.py BinaryCrossEntropyLoss
```

Capture representative frames for the formula, positive-class curve, and negative-class curve.
